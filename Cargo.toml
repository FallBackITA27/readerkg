[package]
name = "readerkg"
version = "0.1.0"
edition = "2024"

[dependencies]
ndarray = "0.17.2"
# rkg-utils = "0.3.0"
rkg-utils = { path = "../rkg-utils/" }
video-rs = { version = "0.11.0", features=["ndarray"] }
